/** the CopilotKit agent id shared by the runtime route and the browser provider */
export const SYSTEM_AGENT_ID = "system";

/** the voice persona: talks, and hands every action to the text agent */
export const VOICE_AGENT_ID = "system-voice";
